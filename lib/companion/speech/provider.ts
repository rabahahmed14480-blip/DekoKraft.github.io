import type{SpeechDocument,SynthesizedSpeech,VoiceProfile}from"./types.ts";
export interface SpeechProvider{readonly id:string;readonly available:boolean;supportedLanguages():readonly string[];listVoices(language?:string):Promise<VoiceProfile[]>;synthesize(document:SpeechDocument,signal?:AbortSignal):Promise<SynthesizedSpeech>;}
export class SpeechProviderRegistry{private providers=new Map<string,SpeechProvider>();private activeId?:string;register(provider:SpeechProvider){if(this.providers.has(provider.id))throw new Error("SPEECH_PROVIDER_DUPLICATE");this.providers.set(provider.id,provider);}activate(id:string){const provider=this.providers.get(id);if(!provider?.available)throw new Error("SPEECH_PROVIDER_UNAVAILABLE");this.activeId=id;}active(){return this.activeId?this.providers.get(this.activeId):undefined;}list(){return[...this.providers.values()].map(provider=>({id:provider.id,available:provider.available,languages:provider.supportedLanguages()}));}}
export const speechProviderRegistry=new SpeechProviderRegistry();
